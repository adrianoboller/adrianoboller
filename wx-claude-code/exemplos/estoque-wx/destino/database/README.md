# database/

Banco PostgreSQL 16. `schema/` é o estado desejado; `migrations/` o caminho até ele, cada uma com par em `rollback/`; `seeds/` só dado anonimizado; `views/` e `procedures/` versionados aqui, nunca só no servidor.
