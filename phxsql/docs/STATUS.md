# Status dos dez recursos — a fonte da página

Esta tabela é a **fonte** de `docs/dossie/status.html`, que **não se edita**:
`python3 docs/dossie/pagina-de-status.py` a gera daqui, e injeta ao lado os
números **medidos** — do `CAPABILITIES.json` e dos `resultados.json` das
bancadas, cada um com a data em que foi medido.

Ela carrega dois tipos de número, e a página os separa de propósito:

- **A nota (0–10) é avaliação**, não medida: saiu da leitura do código e dos
  testes de cada recurso em **08/09/2026**, com as fontes nomeadas na própria
  linha. Quem discordar de uma nota muda **esta** linha, com o motivo, e roda o
  gerador.
- **O painel do alto é medido**: testes, operações, linhas, a bancada de
  gestão, a replicação e o cluster. Nenhum desses se digita aqui.

O «tipo» diz o que a nota significa: `construído` (existe e está provado),
`parcial` (existe, com a metade que falta nomeada), `recusa medida` (não
existe **porque** foi medido que não compensa — não é buraco) e `promessa`
(a documentação prometeu mais do que o código entrega).

| # | recurso | nota | tipo | como está | o que falta |
|---|---|---|---|---|---|
| A | Paginação | 9 | construído | Endereço por conta (`rowid → volume`) em `paginacao.rs`, `.pag` gerado, partição A–Z de 37 baldes, cursor/keyset, `rownum` por bisseção, quatro modos de varredura. Testes em `phxsql-core`, em `phxsql-store/tests/paginacao.rs` e `alfanumerica.rs`, mais os de protocolo; os defeitos históricos estão corrigidos e documentados no `PENDENCIAS.md`. | Comando na CLI — só protocolo e tela (pedido 14). A varredura por índice ainda usa posição, não cursor: decisão documentada em `op_varrer`. |
| B | Replicação | 8 | construído | **Reavaliado em 17/09/2026, segunda vez no mesmo dia** (7 às 04:05, depois de uma primeira onda de conserto; volta a 8, o valor de 08/09/2026, depois da segunda). Diário `.log`, quatro modos, cluster com eleição e promoção automáticas. A bateria de 17/09 (`docs/propostas/bateria-replicacao-2026-09-17.md`) rodou as dez bancadas, **todas verdes**, com ganho sobre a corrida anterior: master **45.117 linhas/s** (1,33× sobre 07/09), trava **83.567 ev/s** (1,90× sobre 05/09), cluster **26 de 26** conferências, quórum com a razão do 2-de-3/3-de-3 mantida. A revisão adversária de SEC (`docs/propostas/revisao-sec-replicacao-2026-09-17.md`) achou 11 furos e o parecer de C (`docs/propostas/parecer-dba-replicacao-2026-09-17.md`) mediu 8 garantias que não valem; **duas ondas de conserto fecharam nove dos onze furos de SEC no mesmo dia**, todos com prova real nos dois sentidos e portões verdes (2.447/0/4, 71 binários): `49a3af7` fechou A2 e A3 inteiros e A1 parcial; `eeb9925` (frente B2) fechou A4, A6, A8, A9, A10, A11 inteiros e A5 por completo, mais o `rownum` do parecer de C (291, via a — `docs/PENDENCIAS.md`). A nota volta ao valor de antes da revisão porque o que ela mediu como buraco de segurança **saiu medido e fechado no mesmo dia**, com o mesmo rigor que o achou. `docs/REPLICACAO.md` §21 e `docs/PENDENCIAS.md` 278–309. | **Ainda aberto, nomeado com pedido**: ~~A1 pleno~~ — **fechado** (pedido 278 ☑, conferido em 24/09/2026 00:0x): a prova do pulso passou a sair do `chave_do_fio`; e o 435 (23/09, `a272d8f`) fechou o oráculo que o próprio 278 abria na mensagem de erro; A7 — `replicas_autorizadas` colapsa num IP só atrás de proxy/NAT, sem conserto ainda (284). **Cinco decisões do dono — TOMADAS em 17/09/2026 07:10 UTC**, e esta linha dizia «ainda não tomadas» seis dias depois (conferido em 24/09/2026 contra o `PENDENCIAS.md`): **implementadas** 289 (duas colunas de sistema, `rowstamp` ordena e `rowtime` empata de propósito) e 292; **decididas e ainda por implementar** 290, 293 e 294 (parecer de C, §6). O que cada uma decidia: coluna de data/hora de sistema por linha e sua resolução, `inicio`/`passo` da `Sequence` no PSCH, o que fazer com único secundário no bidirecional (**travava** o par de servidores para sempre, confirmado pelo soquete — 292 fechado), replicar coluna externa marcada (grava texto cifrado como conteúdo com a cifra desligada — `SEGURANCA.md` §12.4), e o critério de eleição do cluster (a posição somada é um escalar de uma grandeza vetorial). **E uma sexta, menor**: a via (b) do `rownum` — a réplica honrar o número da imagem para também fechar buracos históricos, já gravados antes de hoje (pedido 309). ~~A conferência de continuidade não entrou no bidirecional~~ — **fechado** (pedido 295 ☑). Configuração só à mão no `config.json`, não pela tela. Sem TLS real: a cifra é a Noise-like da casa (`docs/CIFRA-DO-FIO.md`). |
| C | Tabela na memória | 7 | construído | `TabelaMemoria` guarda a linha **decodificada** em RAM com mapa de igualdade por coluna, coerente com o disco na mesma trava e sob o teto `memoria_max_mb`. Doze testes unitários mais a integração ponta a ponta; o ganho sobre o disco está em `DESEMPENHO.md`, medido pelo `examples/memoria.rs`. | Carga e liberação manuais. Mapa só de igualdade — sem min–max para faixa e `BETWEEN`. O pivot decodifica cada linha duas vezes. O catálogo prometia um `mmap` que não existe — corrigido em 08/09/2026. |
| D | Colunar / vetorial tipo HANA | 2 | recusa medida | **Nada colunar existe**: o motor é 100% row-store. Column store como segundo motor foi recusado com número em três documentos (`MEMORIA.md` §4, `VETORES.md` §0, `GPU.md` §7): achatar linha→coluna custa uma passada inteira pela memória, **mais que a varredura que aceleraria**. | É recusa medida, não buraco — reabre só com gargalo analítico medido. A proposta de vetor **de IA** (`VECTOR` + HNSW, `VETORES.md`) é outra coisa, e está «proposta, não construída». |
| E | IA cognitiva / ações automáticas | 3 | promessa | Integração 100% no navegador: o `claude.js` fala com a API; a IA **propõe** — SQL, plano de modelagem, índice — e toda gravação exige um clique. Quatro testes Rust em `http.rs` (CSP liberado, sem chave no binário). | **Nenhuma ação autônoma do motor**, e isso é escolha registrada (`CLAUDE-IA.md` §10), não falta. ~~As 79 provas de navegador da documentação não estão no repositório~~ — **entraram** (pedido 231 ☑). Nunca testado com chave real. |
| F | Backup e restauração | 9 | construído | Backup de dados em zip com manifesto SHA-256 e, desde 08/09/2026, **carimbo do instante** (`quando_ms`); restauração com palco fora da raiz e *rename* atômico; **recuperação a um ponto no tempo** (`restaurar_backup` com `ate`): a cópia restaurada vira réplica do diário vivo até o instante pedido, evento a evento, pelo mesmo aplicador da réplica. Doze testes de PITR com oito sabotagens e a bancada `bancada/pitr/provar.py` (22 conferências pelo soquete, com controle). | Sem ZIP64 — acima de 4 GiB recusa alto. Sem prova de autoria: SHA é integridade, não assinatura. Um banco por vez. PITR exige imagem no diário e recusa `por_cima`; tabela nascida depois da cópia não é recriada (o formato não guarda a história do esquema). |
| G | Comandos SQL | 8 | parcial | `SELECT` com **expressão no `WHERE`** (aritmética, `AND`/`OR`, `IN`, `BETWEEN`, `LIKE`, `IS NULL`, funções), **`GROUP BY`** com `COUNT(*)`/`COUNT(coluna)`/`COUNT(DISTINCT)`/`SUM`/`AVG`/`MIN`/`MAX` e `HAVING`, **`JOIN`/`LEFT`/`RIGHT`/`FULL`/`CROSS JOIN`** em sequência (os quatro últimos pelo pedido 236), **`WITH`**, subconsulta no `FROM`, `IN (SELECT …)`, subconsulta **escalar**, **`ROW_NUMBER() OVER`**, **`CREATE`/`DROP VIEW`**, `INSERT … ON CONFLICT`/`ON DUPLICATE KEY`, `SELECT DISTINCT` (uma ou várias colunas, com `ORDER BY` e `LIMIT`), **`UNION`/`UNION ALL` entre tabelas inteiras**, `ORDER BY` por nome qualificado (`ORDER BY p.id`), parâmetros `?` resolvidos por token, o DML por chave única e, desde 16/09, o **`UPDATE`/`DELETE` por faixa** (`coletar_rowids` colhe as linhas que casam e o servidor aplica `ler`+`atualizar`/`excluir` por linha, com recusa acima do teto). Tudo traduz para operações do protocolo (`varrer.expressao`, `agrupar`, `consultar`, visões) e passa pelo portão único — **cada tabela de dentro de uma junção ou subconsulta é conferida**. Recusas nomeadas, nunca «sintaxe inválida». Os testes da crate estão no painel. | **Planejador** (o primeiro índice compatível vence), o resto da subconsulta **correlacionada** (`IN`/escalar), `WITH RECURSIVE`, mais de uma CTE no mesmo `WITH`, e janela além de `ROW_NUMBER`. Por padrão `READ COMMITTED`; **16/09: leitura repetível passou a existir pela trava, pedida** (`BEGIN ISOLATION LEVEL REPEATABLE READ`/`"leitura_repetivel": true`, `docs/ACID.md` §4.5) — a Sombra continua parada, mas deixou de ser a única via, e `SERIALIZABLE` continua recusado. Era **6** na tarde de 08/09; junções, agregação, subconsultas e visões subiram para 8. **12/09: o `[NOT] EXISTS` correlacionado por apelido de fora (240) passou a rodar — saiu desta lista.** **16/09: o `UPDATE`/`DELETE` por faixa (item 1 do roteiro «SQL para nota 9») passou a rodar — saiu desta lista.** **22/09: `RIGHT`/`FULL`/`CROSS JOIN`, `COUNT(coluna)` e o `ORDER BY` qualificado saíram desta lista — o pedido 236 os fechou e a lista ficou para trás; hoje há prova nos dois sentidos (`right_full_cross_viram_tipo_1_para_1`, `count_de_coluna_sem_distinct_vira_agregado_de_contagem`, `consulta.rs:2123`). No mesmo dia ENTRARAM duas ausências que a lista nunca tinha listado — `WITH RECURSIVE` e mais de uma CTE, as duas recusando nomeando em `consulta.rs:726` e `:748`, e a janela além de `ROW_NUMBER`: lista do que falta também envelhece, e a que só perde item vira propaganda.** **23/09: `DISTINCT` e `UNION`/`UNION ALL` saíram desta lista — traduzem desde `0f7cb29`, com `fmt` limpo, `clippy` zero avisos e 2.647 testes passando. O `DISTINCT` vira `agrupar` e a contagem injetada some no tradutor; o `UNION` vira a op `unir` e **recusa nomeando** o que não cabe nela (filtro, projeção, ordem, junção), apontando o pedido 393 pelo número. **E medindo o `unir` para traduzi-lo apareceram TRÊS defeitos vivos**, o maior deles sendo que o `UNION` devolvia byte a byte o mesmo que o `UNION ALL` — a chave de desduplicação incluía o `rownum`, que é único por linha. O teste que o cobria passava por engano, montando linha de dois valores num esquema de quatro, e o `zip` da chave parava antes das colunas de sistema. Os outros dois: o `pop` cego cortava dado de linha curta, e a escala do `Decimal` do primeiro braço corrompia o segundo (pedido 392, fechado). A nota **continua 8**: o que entrou foi tradução e conserto, e o que segura a nota é outra coisa — o planejador, a correlação que não é igualdade, e o agregado sobre junção (pedido 394 — **fechado depois desta linha**, e sai da lista). **24/09: dois abertos que esta linha não listava** — o **419** (a composição que para no teto publica resultado parcial sem dizer; em curso) e o **426** (o `COMMIT` que esbarra numa migração sai pela metade e manda repetir — ALTO, contrato pronto, espera `servidor.rs`).** |
| H | Stored Procedures | 7 | construído | Guardadas em `base/<database>/procedimentos.json` — **uma por database**, não por tabela. `CREATE`/`DROP`/`CALL`; corpo com `IF`/`WHILE`/`DECLARE`/`SET`/`SIGNAL`/`INSERT`/`SELECT INTO`, decimal exato em `i128`; portão `administrar`. Dezesseis testes mais a prova por soquete (`bancada/rotinas/prova-rotinas.py`: `CALL somar(100)` → 5050). **Funciona.** | Sem `UPDATE`/`DELETE` no corpo; sem `CASE`/`LOOP`, cursor, `FUNCTION`, `CALL` aninhado ou transação. Job com corpo `CALL` funciona, mas **sem teste por soquete**. |
| I | Triggers | 8 | construído | Guardados em `base/<database>/gatilhos.json`. `BEFORE`/`AFTER` × `INSERT`/`UPDATE`/`DELETE`; o `BEFORE` roda **com a trava** (altera `NEW`, cancela por `SIGNAL`, não fala com o motor); o `AFTER` solto (audita, falha vira aviso). Teto de cadeia de 8. A bateria achou e corrigiu **dois defeitos graves** — estouro de pilha e um *deadlock* (`TRIGGERS.md` §9). | O mesmo subconjunto de corpo do item H. Sem `FOLLOWS`/`PRECEDES`. Diferenças em relação ao MySQL(R) documentadas. **E um defeito vivo, medido em 23/09/2026 (pedido 262): o `AFTER` que grava durante o `COMMIT` perde a escrita em silêncio** — o `COMMIT` responde `gravadas:1`, a auditoria fica vazia, e nenhum `gatilhos_avisos` acende (o que FALHA acende; o que grava com sucesso some calado). Os quatro motores maduros convergem 10 a 0 em que a gravação pertence à transação; contrato do conserto pronto em `docs/propostas/parecer-j-262-gatilho-after-no-commit-2026-09-23.md`. **A nota 8 não o desconta ainda** — reavaliar quando o conserto entrar. |
| J | Jobs | 8 | construído | Agendador de 30 s; o job carrega um pedido inteiro do protocolo; `jobs.json` mais `jobs.log` *append-only*. Roda **com o poder do usuário do job** — provado: `so_le` recusado a criar database, `root` cria. Aviso por e-mail (falhou, parado) *opt-in*. Dez testes por soquete mais `prova-avisos.py` com SMTP falso. | Sem cron completo — a agenda é o enum `Agenda`. Job com corpo `CALL` sem teste por soquete. |

## Leitura

O núcleo **de armazenamento** — paginação, replicação, backup, jobs, triggers —
está entre 8 e 9: construído, medido e provado por soquete, com defeitos de
produção já caçados (a replicação oscilou entre 8 e 7 dentro do próprio
17/09/2026: desceu quando uma revisão de segurança e um parecer de DBA
mediram furos e garantias que não valem, e voltou a 8 no mesmo dia depois de
duas ondas de conserto fecharem a maior parte deles — ver a linha B). A
**camada SQL** deixou de ser o ponto fino em 08/09:
junções, agregação, subconsultas e visões entraram, e o que falta ali é o
planejador e as formas correlacionadas. Sobram os dois pedidos que hoje quase
não existem: colunar tipo HANA e IA autônoma. E a distinção que importa: dessas
duas notas baixas, **D é recusa medida** (o número diz que o colunar custaria
mais do que renderia), enquanto **E é promessa maior que a entrega** — a UI
assistiva funciona, mas «ações automáticas» e as provas documentadas não
estão no código.

**17/09/2026 —** a rodada da noite de 16/09 não moveu nota nenhuma das dez, e isso fica dito em vez de ficar implícito pela data. O que ela moveu é transversal e não cabe numa das dez linhas: nove structs de configuração, usuários, DbLink, SQL e ODBC que escreviam segredo no `Debug` derivado (`docs/SEGURANCA.md` §16, pedido 270), a quinta régua do catálogo de guardas (`docs/CATRACAS.md` §12, pedido 269) e as nove guardas da pétrea da senha (§15.7). As notas de B (a `Origem` e o `Cluster` da replicação estavam entre as nove) e de G (o `Comando` de usuário do SQL também) não mudam por isso: o que vazava era a saída de depuração, não o comportamento que a nota avalia.
