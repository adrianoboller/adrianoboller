# Parecer do DBA (papel C): pedido 448, segunda revisão — 24/09/2026

Sonda fora da árvore da frente (crate de medição em release, dependência de caminho). **Antes** = `82a17ef` (`git archive`); **depois** = o worktree `agent-a2dab1f4a8bef6045` com `table.rs` `5faf826c`, `servidor.rs` `455f65fa` e `transacao.rs` `c109cf82` (md5). Suíte store+server no mesmo worktree: **2.178 passam, 0 falham**.

**Veredito: LIBERA.** Os cinco bloqueios da primeira revisão caíram, medidos. Nenhuma regressão de dado gravado contra o `82a17ef`. Duas condições são de texto e entram **no mesmo commit** (C1 e C2). Cinco defeitos ativos entram na conta. Quatro deles já existiam no `82a17ef`, medidos iguais; o quinto (N5) o A4 abriu num relatório, e não toca dado gravado. Nada sobe ao dono.

## O que a frente afirmou, conferido

| Item | Cenário | 82a17ef | v2 |
|---|---|---|---|
| A1 | `[inserir pedido→A, alterar código de B 2→3]`, B sem filha | pela metade, «DEFEITO DO MOTOR» | COMMITTED; a recuperação reaplica sem mudar nada |
| A1 | `[inserir filha→5, mãe 5→6]` | recusa depois da marca | COMMITTED |
| A2 | n filhas na lista + n alterações de chave, COMMIT (mediana de 3) | 195,6 / 400,8 / 828,8 ms | 720,9 / 1.460,1 / 3.264,2 ms (n = 2k/4k/8k) |
| A3 | mãe com código pelo DEFAULT 7 + filha→7 | COMMITTED | COMMITTED; a busca na tx acha a mãe |
| A3 | alterar só o nome, sem a Sequence, + filha→código | COMMITTED; a leitura na tx mostra `codigo:null` | COMMITTED; a leitura na tx mostra o código |
| A4 | NULL no disco + `[x, NULL]` na tx | 1 gravada, «DEFEITO DO MOTOR» | COMMITTED, 4 linhas; `x` repetido recusa |
| A4 | banco do binário velho (1 NULL) aberto pelo novo | — | `reindexar` e `verificar` OK; índice `desc` OK |
| M3 | mensagens | — | conferidas; a do «outra sessão» diz o que fazer e o conselho resolve |

Leitura do código:

- **A2** é linear: não há cópia da sobreposição no caminho. O `Arc::make_mut` só copia com `Arc` dividido, e o plano solta a filha antes do `dobrar`.
- **A3** não consome contador, e isso está provado pelo tipo: `ContadorPrevisto` só tem `&RegFile`, e `rownum_para` recebe `&self`.
- **A1**: o `costurar_os_elos` (`servidor.rs:125`) marca toda alteração com `cascata_na_lista`. A passada (`servidor.rs:16806`) e a recuperação (`transacao.rs:1767`) aplicam sem replanejar.
- **Formato:** os bytes do `.ndx`, do `PSCH` e da marca v3 não mudam. A marca de um binário velho continua pelo caminho do `recascatear`.

**`pre_conferencia_desligada` NÃO é campo de produção.** O campo, a iniciação e a leitura estão atrás de `#[cfg(test)]` (`servidor.rs:1146`, `:1454` e `:16135`). O caminho `not(test)` fixa `true`. Nenhuma op, configuração ou rede alcança o campo, e nada muda aqui.

## Condições (texto, mesmo commit)

**C1 — `FORMATO.md`: o índice único mudou de invariante, e o binário anterior não sabe disso.** Medido, com o banco criado pelo `82a17ef`, 2º NULL posto pelo v2 e o banco reaberto pelo `82a17ef`:

- o `reindexar` recusa, com «chave repetida no lote»;
- o `.ndx` fica marcado «ficou para trás numa queda», e toda operação na tabela dá `CORROMPIDO`;
- o v2, reaberto, reindexa e volta.

Escrever três coisas: chave com componente NULL não colide desde o 448; **não há volta de versão** com dois NULL num índice único; e a réplica com binário velho para no 2º NULL, então as réplicas se atualizam antes da origem.

**C2 — `ACID.md` §2.5 afirma mais do que vale.**

- «A filha que a própria lista escreveu acompanha a mãe» vale para a filha que a lista **INSERIU**. Para a que a lista **ALTEROU** antes da mãe, o elo planejado no `empilhar` sobrescreve a escrita (N2).
- «Chave estrangeira que falha sai com zero gravado» precisa dizer que a coluna de FK preenchida por DEFAULT ou calculada **nunca é conferida** (N1).

## Na conta — defeitos ativos, não regressões do 448 (salvo o N5)

**N1 (ALTO): a FK é conferida ANTES do DEFAULT e da coluna calculada, e grava órfã.**

- Medido igual antes e depois:
  - pedido com `cod_cliente` pelo DEFAULT 7, sem mãe 7, gravado fora e dentro da transação;
  - item com `cod_cliente` calculado (`x+0`) gravado com 9 e alterado para 8, sem mãe nenhuma.
- Onde: `conferir_as_maes` com a linha crua em `table.rs:4503` e `:4858`, antes de `completar`/`aplicar_regras` (`:4508`–`:4536`). A pré-conferência herda a ordem em `table.rs:3559` e `:3568`.
- Fere «chave declarada nasce conferida». PG, MySQL e MariaDB conferem a linha final: aceite automático.

**N2 (ALTO): o elo que o `empilhar` planeja pelo disco sobrescreve o que a própria lista já escreveu na filha.**

| Lista | PG | 82a17ef | v2 |
|---|---|---|---|
| `[filha id 10→11, mãe 5→6]` | id 11, cód. 6 | id 10 | id 10 |
| `[filha troca de mãe 5→8, mãe 5→6]` | cód. 8 | cód. 6 | cód. 6 |
| `[excluir suave a filha, mãe 5→6]` | filha excluída | filha ressuscita | filha ressuscita (é o gatilho do 492 pelo elo) |
| `[excluir de vez a filha, mãe 5→6]` | COMMIT | pela metade | recusa com zero gravado (melhor) |

- Onde: o plano do `empilhar` sai só do disco (`servidor.rs:15270` e `:15414`). A pré-conferência tem o plano certo (disco + prefixo) e o descarta como «adiante» em `servidor.rs:16465`.
- A FK continua íntegra em todos os casos traçados. O dado não.

**N3 (MÉDIO): o elo implícito ignora a trava de outra transação.**

- T1 muda a chave da mãe. Depois nasce a filha, e T3 abre com `leitura_repetivel` e lê cód. 5.
- O COMMIT de T1 leva o elo, e T3 relê **6**: a leitura repetível pedida quebra.
- Com T3 escrevendo a filha, a escrita dela é recusada no COMMIT, sem dano.
- Igual no `82a17ef`. Quando a filha já existe no `empilhar`, a fase 2 trava e T1 espera; só o elo implícito passa por fora.

**N4 (MÉDIO, por leitura): o bidirecional aceita índice único sobre coluna ANULÁVEL como identidade.**

- `bidirecional.rs:163` não exige NOT NULL, e `servidor.rs:5284` aplica em `achadas.first()`: NULL casa com NULL e sobrescreve a linha de outro.
- Já existia entre nós. O A4 abre o caso dentro de um nó.
- O PG exige NOT NULL no REPLICA IDENTITY USING INDEX.

**N5 (MÉDIO, aberto pelo A4): o `diff` por índice único com NULL repetido some com linhas.**

- Medido: a = {ana, bia} e b = {ana, caio}, todos com e-mail NULL.
- Saída: `iguais 0`, `so_em_a [[null]]`, `so_em_b []`, e ana×caio em `diferentes`. A linha b.ana some (`diferencas.rs:135`).
- Chave com NULL não pode casar.

Seguem abertos e medidos iguais: **491** (auto-referência; o v2 consertou `[chefe 20, sub 21→20]`, que antes saía pela metade) e **492** (ganha o gatilho do N2).

## ⏸ — os limites que a frente declarou, julgados

- **`varrer` por índice dentro da transação** põe a linha alterada na posição da chave velha: `[99,2,3,4,5]` antes e depois. A existência está certa; é ordem, e da mesma classe da nascida no fim. O `buscar` ficou **certo** no v2: antes achava pela chave velha com o valor novo. O `intervalo` não tem chamador no servidor.
- **`rowstamp`/`rowtime` não previstos:** o índice sobre coluna de sistema não se declara (medido: «índice usa coluna inexistente»). Sobra a leitura na transação mostrando 0/1970 na nascida. CHECK ou DEFAULT sobre coluna de sistema recusaria na pré-conferência (não medido).
- **Elos implícitos fora do `transacao_max_linhas`:** memória e marca crescem com a cascata. É da família do 494. A parte da trava é o N3.
- **Custo, família do 494:**
  - A2 fica em 3,6–3,9× (360–410 µs por alteração);
  - M1 dá 2,03× com 10.000 filhas (710,5 → 1.438,8 ms, faixas não se cruzam). O «abaixo de 2×» do `ACID.md` vale para a carga da frente, não para esta;
  - M2 dá +105 MB contra +10 MB com 100.000 escritas.
- **Cópias latentes da regra da unicidade:**
  - `NdxFile::inserir` (`ndx.rs:1307`) e `construir_em_lote` (`:1570`) ainda fazem NULL colidir; só o `.fts` e os testes as usam;
  - o upsert decide o NULL pelo valor (`upsert.rs:313`, `servidor.rs:14975`) e diverge só em índice por expressão;
  - o `reindexar` repete o predicado numa closure (`table.rs:6930`).
- **O elo implícito passa a disparar o AFTER UPDATE da filha dentro de transação** (`servidor.rs:16849`), como o elo explícito já disparava desde o ACID-C; o `atualizar` solto não dispara. A matriz dos motores parece empatar (PG e SQLite disparam; MySQL e MariaDB documentam que não): o papel J confere antes de virar pergunta.
