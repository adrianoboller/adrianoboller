# Parecer do papel C — frente 509+512 (fsync recusado e página suja do `.ndx`)

24/09/2026 · worktree `agent-a9a4f50ee5c06e476`, não comitado, sobre `686dc59` · só leitura, provas rodadas por mim ·
origem: C1 e C2b do `parecer-dba-496-catastrofes-2026-09-24.md`.

## Veredito: **LIBERA COM CONDIÇÃO**

O código entra: o 512 está provado contra o SO **no meio do lote, nos dois sentidos**; a repetição do `fsync` no
mesmo processo acabou; o 503(2) fechou. **O 509 não fecha**: a repetição mudou de processo. Medido 3/3: depois do
`abort`, o arranque no **mesmo boot** reaplica 0 de 5.000 linhas (o cache do núcleo devolve o que o disco perdeu),
o `sincronizar` responde Ok e a marca sairia; remontado, o dado não está lá. As três condições são **texto**.

## 1. O que rodei

| # | prova | contra | resultado |
|---|---|---|---|
| T | `disco-que-recusa` da frente (comando dela, `cargo-da-frente.sh`) | forjado | **6/6**; o binário repetido **30/30**, sem floco |
| P3 | 512 no **meio** do lote: tmpfs, 20.000 linhas, enche deixando 40 KiB, fecho 1, libera, fecho 2 no mesmo punho | SO | worktree: fecho 1 ENOSPC com **48 de 680 KiB** do `.ndx` alocados (≈10 de 170 páginas); fecho 2 Ok → 680 KiB, `verificar` Ok, **20.000/20.000** pela chave (3/3). **HEAD `686dc59`** (defeito reposto): fecho 2 Ok com os mesmos 48 KiB, byte 52 = 0, `CRC invalido na pagina 167`, **0/20.000** (3/3) |
| P1 | 509 depois do `abort`: provisionamento fino; 5.000 inserções; fecho com o gancho; operador libera; **arranque no mesmo boot** com a decisão do `aplicar_uma` (`transacao.rs:1820`: `rowid <= slots && ler(rowid).is_some()` → pula) e o `sincronizar` do `completar`; remonta | SO | `abort` 134 (3/3). Arranque: **pula 5.000, reaplica 0, `sincronizar` Ok** (3/3) — o `recuperar` apagaria a marca. Remontado: `.log` ilegível («operacao desconhecida no log: 0»), `.ndx` byte 52 = 0 com `CRC invalido na pagina 3`, `.reg` com 19/5.000 legíveis em 1 das 3 |
| P1' | variantes, 1 rodada cada | SO | **disco ainda cheio**: o arranque cai de novo, pelo erro do `.reg` que ninguém tinha visto — laço de quedas que termina no Ok do P1 quando o espaço volta. **Remontado antes do arranque** (= reboot): pula 5.000, Ok, `.ndx` fica corrompido com byte 52 = 0 |
| P4 | o `fechar` sem `fsync` (achado da frente), com controle: 5.000 inserções, `syncfs` com o disco cheio, **nenhum `fsync` do motor**, remonta | SO | com `Drop`→`fechar`: byte 52 = **0**, `CRC invalido na pagina 3`, `precisa_reconstruir=false` (3/3). Controle, processo cai sem `Drop`: byte 52 = **1**, `precisa_reconstruir=true` (3/3) |
| P2 | chave da recusa por diretório (biblioteca, recusa forjada) | forjado | relativo↔absoluto: segura. Bases vizinhas: não se contaminam (desenho). **Symlink** e **`b/../b`→`b`**: a outra grafia sincroniza **Ok**; pela tabela no symlink, `reindexar` + fecho baixam o byte 52 no processo da recusa |
| G | `mapa-da-trava.py --catraca` | fonte | `alcancam-fsync-2` **23** (teto 23), igual ao HEAD |

O P1 **emula** a decisão do `recuperar` pelo `phxsql-store` (a condição do `aplicar_uma` e o `sincronizar`, lidas no
fonte); o `phxsqld` inteiro não rodou.

## 2. As seis perguntas

| | resposta |
|---|---|
| **1. Garantia cai?** | **No processo, não.** Todo `fsync` de dado passa pelo `sincronia::sync_all` (grep: 0 `sync_all` direto no `phxsql-store` fora dele); o `recusar` roda antes do gancho, então o `Drop` de outra thread na janela até o `abort` já lê a recusa; o gancho é a 1ª linha do `Servidor::novo` e o `main` não toca o store antes. Cobre fecho da janela e seus fios, COMMIT, arranque, réplica, expurgo da trilha (`sincronizar_volume`), restauração e `reindexar`→`sincronizar`. **No processo SEGUINTE, cai** (P1). E cai uma promessa escrita: `SAUDE-DO-DISCO.md:22` e `:82-86` dizem que o `fsync` recusado do fecho vira evento da saúde (e o e-mail); agora o processo cai antes do `fecho_recusado` — **o aviso sumiu** e o documento não mudou. |
| **1b. fsync fora do store** | Quatro. **Marca do COMMIT** (`transacao.rs:1150`): não repete (arquivo novo por COMMIT) nem engole, mas fica **fora da regra** — recusada ali, o servidor segue de pé confirmando no mesmo diretório (é o 503(3); texto em §5). `config.rs:2706/2775`: tmp+fsync+rename, o erro sobe — ok. `saude_do_disco.rs:579`: a sonda regrava o mesmo nome e repete o `fsync` a cada passada (⏸). `phxsql-core`: nenhum. E o **`backup.rs` não tem `fsync` nenhum** (0), enquanto `volume.rs:1347` diz «com `sync_all` próprio» — pedido novo. |
| **2. Por diretório** | Dois bancos = dois diretórios: um não envenena o outro (a queda do servidor torna isso irrelevante lá). Relativo↔absoluto casa (`absoluto_lexico`). **Symlink e `..` não casam** — a chave é léxica, sem `canonicalize` (pedido novo, só biblioteca). Vazamento entre testes: a lista de recusas compara por componente e o `DirTemp` é único → não vaza; a **arma** de teste é prefixo de TEXTO (medido: armar `loja` derruba `loja2`) — sem colisão hoje (rótulo+sequência), frágil (⏸). |
| **3. 512 no meio do lote** | **Sim**, provado contra o SO nos dois sentidos (P3). `sujas()` copia, `gravada(n)` só depois do `write`; a que falha e as seguintes ficam sujas; o selo no lugar não estraga a devolução (a cifra vai em buffer novo, `ndx.rs:1066`). O despejo recusado fica acima do teto por **uma página por despejo recusado** enquanto o disco recusa (limitado às páginas do arquivo), e não «uma página» como diz o comentário (⏸). |
| **4. `fechar` sem `fsync`** | **Confirmado, com controle** (P4): a causa é o `fechar`, sem `fsync` nenhum do motor — o 509 nem tem recusa para ver. **É formato**: a regra 2 do byte 52 (`FORMATO.md`, «só volta a 0 depois de todas as páginas sujas terem ido ao disco») vale para a queda do processo, não para a escrita de fundo recusada nem para a queda da máquina. Não muda leiaute nem pede migração: muda **quando** o 0 se grava. **Entra cedo. Não bloqueia esta frente** (é anterior, ela não piora, e o diff do `FORMATO.md` já o diz); vira pedido próprio — e é **pré-requisito** do arranque certo do 509 (P1': com o byte 52 em 0, o arranque não reconstrói). |
| **5. Core dump (504)** | **Aumenta a exposição.** O gatilho do `abort` deixa de ser defeito interno (H5) e passa a ser o **disco** — externo, alcançável por quem enche um volume de provisionamento fino. Mitigação no código: 0 (`RLIMIT_CORE`, `coredump_filter`, `PR_SET_DUMPABLE`: nenhuma ocorrência). Não medido: aqui `core_pattern=core` e `ulimit -c 0`, e nenhuma das quedas das provas deixou core. |
| **6. Catracas** | `alcancam-fsync-2` em 23, igual ao HEAD. Nenhum `fsync` novo sob a trava: os sítios são os mesmos, e o nome `sync_all` foi mantido para o mapa achar. |

## 3. Condições (texto; sem elas não integra)

| # | o quê | por quê |
|---|---|---|
| C1 | O 509 fica **☐**, com o acréscimo de §5: a frente fecha a metade do processo | P1: o estado final que o 509 descreve se repete com um reinício no meio |
| C2 | Tirar as afirmações de que o arranque completa as marcas: a mensagem do `fsync_recusado_derruba_o_processo` (`servidor.rs:26845`, «o arranque completa as marcas de COMMIT que ficaram»), o doc dele (`:26836`), o `sincronia.rs:54` («deixar o bilhete — a marca, o byte 52 — para quem sobe depois») e o comentário do teste do servidor (`:55940`), que prova o **fluxo** com recusa forjada, sem página perdida. A mensagem passa a dizer o medido: no mesmo boot o cache devolve o que o disco perdeu; remonte o volume ou reinicie a máquina antes de subir | no P1 os dois bilhetes falham: o byte 52 já estava em 0 (P4) e a marca sai no arranque. Comentário que se declara resolvido é o motivo de ninguém olhar de novo |
| C3 | `SAUDE-DO-DISCO.md:22,82-86`: o `fsync` recusado do fecho passa a derrubar o processo e **não** vira mais evento nem e-mail; o aviso volta com o arranque do 509 | promessa escrita que a frente derrubou sem mexer no documento |

## 4. ⏸ (visível, fora da conta) — linhas prontas

```text
| ⏸ | — | **A sonda da saúde do disco repete o fsync no mesmo nome, e pode voltar a «saudável» depois de um EIO** | Papel C, revisão 509+512, 24/09/2026, **lido**: `saude_do_disco.rs:568-579` trunca e regrava o mesmo canário a cada passada; depois de um EIO o fsync seguinte responde Ok pelo mesmo motivo do 509. Sinal, não dado: o evento anterior fica. |
| ⏸ | — | **A arma de teste do 509 é prefixo de TEXTO: armar `loja` derruba `loja2`** | Papel C, revisão 509+512, medido (parecer, P2). Sem colisão hoje (rótulo + sequência do `DirTemp`); quem criar rótulo que seja prefixo de outro herda falha forjada alheia. |
| ⏸ | — | **O gancho do `abort` é do processo: teste do servidor que armar `Fsync` sem subir filho derruba a bateria inteira** | Papel C, revisão 509+512, lido: `OnceLock` registrado no `Servidor::novo`; hoje o único teste que arma está num filho. Falha ruidosa, não calada. |
| ⏸ | — | **Despejo recusado deixa o cache uma página acima do teto POR recusa, e não uma no total** | Papel C, revisão 509+512, lido (`ndx.rs`, `por`/`devolver`): limitado às páginas do arquivo; o comentário do `devolver` diz «uma página». |
| ⏸ | — | **Na biblioteca a recusa do 509 morre com o processo: quem repete o `restaurar` pela linha de comando recebe o Ok do núcleo** | Papel C, revisão 509+512, lido. É o meio do SQLite (peso 1 na régua); a mensagem da recusa deveria dizer «remonte ou reinicie antes de repetir». |
```

## 5. Pedidos — texto pronto (números sugeridos; o integrador confere o próximo livre)

**509, acrescentar ao fim do corpo:**
> **Parcial pela frente 509+512 (24/09/2026):** no processo acabou — o `fsync` recusado derruba o servidor e a
> biblioteca recusa o diretório até reiniciar. **Continua aberto, medido pelo papel C
> (`docs/propostas/parecer-dba-509-512-2026-09-24.md`, P1, 3/3):** o supervisor sobe no MESMO boot, o cache do núcleo
> devolve as 5.000 linhas que o disco perdeu, o `aplicar_uma` pula as 5.000, o `sincronizar` responde Ok e o
> `recuperar` apagaria a marca; remontado, `.log` ilegível e `.ndx` com CRC inválido e byte 52 = 0. Com o disco ainda
> cheio o arranque cai de novo pelo erro que ninguém viu, e o laço termina no mesmo Ok. E o aviso sumiu: a saúde do
> disco não recebe mais o `fsync` recusado do fecho. Direção: o gancho grava, antes do `abort` e sem `fsync` (quem
> precisa ler é o mesmo boot, pelo mesmo cache), uma sentinela na instância com o `boot_id` e as tabelas sujas; o
> arranque que a acha no mesmo boot não confia no cache — (a) recusa subir dizendo como (remontar/reiniciar), ou (b)
> despeja o cache desses arquivos (`posix_fadvise(DONTNEED)` ou leitura `O_DIRECT`, sem crate) e reaplica forçando a
> regravação no mesmo slot, com `reindexar` das tabelas da sentinela; o evento da saúde sai dali. Arquivo novo na
> instância: formato, entra cedo, `FORMATO.md` no mesmo commit. Depende do 522. Papel J nos quatro antes de escolher;
> se ganhar (a), é SLA e sobe ao dono.

**522 (novo):**
```text
| ☐ | 522 | **O `fechar` baixa o byte 52 SEM `fsync`: com a escrita de fundo recusada (ou a máquina caindo), o `.ndx` fica marcado LIMPO sobre página que não chegou ao disco** | Achado da frente 509+512, confirmado pelo papel C em 24/09/2026 (`docs/propostas/parecer-dba-509-512-2026-09-24.md`, P4), **provado contra o SO com controle**: ext4 sobre loop com provisionamento fino, 5.000 inserções, `syncfs` com o disco cheio e NENHUM `fsync` do motor; remontado, com o `Drop` (que chama o `fechar`) o byte 52 = 0, `CRC invalido na pagina 3` e `precisa_reconstruir=false` (3/3); o processo que cai sem `Drop` deixa byte 52 = 1 e o recado certo (3/3). A regra 2 do `FORMATO.md` vale para a queda do processo, não para a da máquina: o cabeçalho é a página 0 e nada o ordena depois das outras. **Formato, entra cedo**, sem leiaute novo nem migração — muda QUANDO o 0 se grava. Direção: só o `sincronizar` (depois dos dois `fsync`) baixa o byte 52; o `fechar` leva as páginas ao núcleo e deixa o 1, e um registro do processo (como o `familias_devendo_em`) diz ao punho reaberto que o 1 é coerente no núcleo; preço: depois de queda do processo, `reindexar` das tabelas tocadas desde o último fecho da janela. Medido: CRC inválido, ruidoso. **Não medido**: queda de energia com página velha de CRC válido — aí o índice mentiria calado, e a FK conferida pelo índice deixaria excluir pai com filho (regra primordial). Pré-requisito do arranque do 509. |
```

**523 (novo):**
```text
| ☐ | 523 | **A recusa do 509 na biblioteca é por GRAFIA do caminho: pelo symlink ou por `dir/../dir` o mesmo diretório sincroniza Ok depois de um `fsync` recusado** | Papel C, revisão da frente 509+512, 24/09/2026 (parecer, P2), recusa forjada: o diário recusado por `real/` sincroniza Ok por `link/`, e `b` sincroniza Ok depois de recusar por `b/../b`; pela tabela no symlink, `reindexar` e fecho baixam o byte 52 no processo da recusa. Relativo↔absoluto segura. Só a biblioteca (FFI, ferramentas): o servidor cai na primeira recusa. Conserto: `canonicalize` do diretório no `recusar` e, só com `HA_RECUSADO`, no `recusado_em` — o caminho de sempre não paga nada. |
```

**524 (novo):**
```text
| ☐ | 524 | **O backup não faz `fsync` de nada, e o comentário que conta os caminhos de escrita diz que faz** | Papel C, revisão da frente 509+512, 24/09/2026, **lido**: `backup.rs` tem 0 `sync_all` — cópia e manifesto por `std::fs::write` (`:325`, `:334`) — e `volume.rs:1347` diz «`backup.rs` (cópias, com `sync_all` próprio)». Backup que responde «concluído» pode não estar no disco do destino, justamente quando o operador apaga o anterior; e fica fora do 509, porque sem `fsync` não há recusa para ver. Conserto: `sincronia::sync_all` em cada cópia e no manifesto, manifesto por último (+ o `fsync` do diretório do 467); o comentário no mesmo commit. |
```

**503, acrescentar ao item (3):**
> Papel C, revisão 509+512: o (3) se conserta pela régua que a frente usou no 509 — passar o `sync_all` da marca
> (`transacao.rs:1150`) pelo `phxsql_store::sincronia::sync_all`, e a recusa derruba o servidor. PostgreSQL: PANIC
> incondicional no `fsync` do WAL (`xlog.c:8782-8792`, REL_17, `issue_xlog_fsync`, conferido no fonte); InnoDB cai no
> do redo (`os_file_fsync_posix` → `ib::fatal`, o mesmo do 496); SQLite devolve erro: **9 × 1, cair**. Cair troca a mentira de hoje («abortada» ao cliente, marca aplicada no
> arranque) por «em dúvida». Hoje a marca é o único `fsync` de dado do servidor fora da regra do 509.

**504, acrescentar:**
> Papel C, revisão 509+512: a frente acrescenta um gatilho EXTERNO ao `abort` — o `fsync` recusado, alcançável por
> quem enche um volume de provisionamento fino —, onde antes só havia defeito interno (H5). A exposição sobe.
> Mitigação no código: nenhuma. Aqui `core_pattern=core` e `ulimit -c 0`: nenhuma das quedas das provas deixou core —
> segue não medido onde importa (systemd-coredump). Deve entrar na mesma versão que leva o `abort` do 509.

## 6. Sobe ao dono?

**Nada agora.** Sem choque com pétrea (ordem de digitação intacta: a reaplicação forçada regrava o **mesmo** slot),
sem empate (10×0 e 9×1), sem produto. Pode subir **uma** coisa depois: se o J escolher, para o arranque do 509, «não
subir sozinho no mesmo boot», isso é SLA.

**Não medido:** o `phxsqld` inteiro (P1 emula o `recuperar`); o `Atualizar` da marca (se regravar valor igual suja a
página); a página velha de CRC válido na queda de energia; o core dump.

---

## Apêndice — para refazer (root e `unshare -m`; as provas moram fora do repositório)

Crate `prova509` com `phxsql-core` e `phxsql-store` por caminho do worktree; `prova509head` o mesmo sobre
`git archive 686dc59 phxsql/Cargo.toml phxsql/Cargo.lock phxsql/crates`, sem o `p2` e sem o gancho. Compilado com
`CARGO_PROFILE_DEV_DEBUG=0 ./cargo-da-frente.sh build --offline --manifest-path .../Cargo.toml --target-dir ...`.

```bash
unshare -m --propagation private bash p3.sh $S                                  # P3, worktree
P=$S/target-head/debug/prova509head unshare -m --propagation private bash p3.sh $S   # P3, HEAD
for m in mesmo-boot-liberado mesmo-boot-cheio reboot; do unshare -m --propagation private bash p1.sh $S $m; done
for m in inserir inserir-e-cai; do unshare -m --propagation private bash p4.sh $S $m; done
$S/target/debug/prova509 p2 $S/p2                                               # P2, sem privilegio
```

`p1.sh` (o `p4.sh` é o mesmo até o `mount` final, depois roda `$P $MODO $E/db 5000`, `sync -f $E` com o disco cheio,
libera, remonta e confere):

```bash
set -u
S="$1"; MODO="$2"; P="$S/target/debug/prova509"; N=5000
B="$S/p1back"; E="$S/p1ext"; IMG="$B/disco.img"; mkdir -p "$B" "$E"
mount -t tmpfs -o size=80m tmpfs "$B"
truncate -s 64M "$IMG"; mkfs.ext4 -q -F -N 64 -J size=4 "$IMG"; fallocate -l 64M "$IMG"
LOOP=$(losetup -f --show "$IMG")
trap 'umount "$E" 2>/dev/null; losetup -d "$LOOP" 2>/dev/null; umount "$B" 2>/dev/null' EXIT
mount -t ext4 "$LOOP" "$E"; "$P" criar "$E/db"; sync; umount "$E"
for f in $(dumpe2fs "$IMG" 2>/dev/null | sed -n 's/^ *Free blocks: //p' | tr ',' '\n' | tr -d ' '); do
  [ -z "$f" ] && continue; a=${f%-*}; b=${f#*-}; fallocate -p -o $((a*4096)) -l $(((b-a+1)*4096)) "$IMG"; done
livre=$(df -k --output=avail "$B" | tail -1); dd if=/dev/zero of="$B/enchimento" bs=1k count=$((livre-64)) 2>/dev/null
mount -t ext4 "$LOOP" "$E"
"$P" inserir "$E/db" $N
ABORTA=1 "$P" fecho "$E/db"; echo "saida=$?"
case "$MODO" in
  mesmo-boot-liberado) rm -f "$B/enchimento";;
  mesmo-boot-cheio) ;;
  reboot) rm -f "$B/enchimento"; umount "$E"; mount -t ext4 "$LOOP" "$E";;
esac
ABORTA=1 "$P" arranque "$E/db" $N; echo "saida=$?"
rm -f "$B/enchimento"; umount "$E"; mount -t ext4 "$LOOP" "$E"
"$P" conferir "$E/db" $N
```

`p3.sh` (3 rodadas; `K=40`): `mount -t tmpfs -o size=64m`; `$P p3 $T/db 20000 $SIG &`; espera `inserido`; `dd` até
sobrar `K` KiB; toca `cheio`; espera `fecho1`; apaga o enchimento; toca `liberado`; `wait`; `umount`.

Os modos do `prova509` que não estão no `disco-que-recusa` da frente (esquema, `linha` e `conferir` são os dela, com
`ler` por rowid e `st_blocks` a mais):

```rust
// o gancho do servidor, sem o servidor
if std::env::var_os("ABORTA").is_some_and(|v| !v.is_empty()) { sincronia::ao_recusar(abortar); }
"inserir-e-cai" => { /* insere N e chama std::process::abort() sem Drop: o controle do P4 */ }
// a decisao do `aplicar_uma` (transacao.rs:1820) e o `sincronizar` do `completar`
"arranque" => {
    let n: i64 = a[3].parse().unwrap();
    let mut t = Table::abrir(&d, "pedidos").unwrap();
    let slots = t.slots();
    let (mut pula, mut reaplica, mut impossivel) = (0, 0, 0);
    for rowid in 1..=n as u64 {
        if rowid <= slots { match t.ler(rowid) { Ok(Some(_)) => pula += 1, _ => impossivel += 1 } }
        else { reaplica += 1; }
    }
    println!("arranque: slots={slots} pula={pula} reaplicaria={reaplica} impossiveis={impossivel}");
    if t.indice_precisa_reconstruir() { println!("reindexar={:?}", t.reindexar().map(|_| ())); }
    println!("arranque sincronizar={:?}", t.sincronizar());
}
"p3" => { /* criar; N insercoes; toca inserido; espera cheio; fecho1 (imprime st_blocks do .ndx);
             toca fecho1; espera liberado; fecho2 no MESMO punho; drop; conferir */ }
// P2: um LogFile recusado por uma grafia e sincronizado por outra
let caso = |rot: &str, recusa: &Path, depois: &Path| {
    let mut l = LogFile::criar(recusa, "t", Paginacao::DESLIGADA).unwrap();
    l.sincronizar().unwrap();
    for rowid in 1..=50 { l.registrar(Operacao::Inclusao, rowid, 1).unwrap(); }
    falha_de_teste::armar(recusa, Onde::Fsync, 1);
    let (f1, f2) = (l.sincronizar(), l.sincronizar());
    drop(l);
    let mut m = LogFile::abrir(depois, "t", Paginacao::DESLIGADA).unwrap();
    m.registrar(Operacao::Inclusao, 51, 1).unwrap();
    println!("[{rot}] {:?} {:?} fecho3={:?}", f1.is_ok(), f2.is_ok(), m.sincronizar().is_ok());
};
```

O lugar certo destes modos é o `examples/disco-que-recusa.rs` da frente, e os cenários no `bancada/catastrofes/prova.sh`
— roteiro que resolveu algo não morre com a sessão.
